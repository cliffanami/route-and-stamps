# Broadsheet (pending)

This directory is where the vendored design-system handoff goes, as-is,
per ARCHITECTURE.md §1b:

- `styles.css`
- `theme.json`
- `print-plates.js` (optional at MVP — see ARCHITECTURE.md §1b)
- `broadsheet-guide.md`

Not yet provided. Once these files are added:

1. Import `styles.css` once, globally, from `src/app/globals.css`.
2. Read `broadsheet-guide.md` in full before building any UI component.
3. Delete this README.
